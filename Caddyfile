{$DOMAIN:localhost} {
    {$CADDY_TLS:tls internal}

    # Login page + auth API -> Management API on host
    handle /login {
        reverse_proxy host.docker.internal:9998
    }
    handle /api/auth/* {
        reverse_proxy host.docker.internal:9998
    }

    reverse_proxy openclaw:18789
}
