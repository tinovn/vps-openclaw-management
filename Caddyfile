{$DOMAIN:localhost} {
    {$CADDY_TLS:tls internal}

    # Auto-inject gateway token via hash fragment for new tabs
    @needs_token {
        not query token=*
        not header Connection *Upgrade*
        path /
        method GET
    }
    redir @needs_token /#token={$OPENCLAW_GATEWAY_TOKEN} 302

    reverse_proxy openclaw:18789
}
